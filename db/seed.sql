const db = require('../db/schema');
const movie = require('../models/Movie');
const user = require('../models/User');

CREATE TABLE watchlist;

INSERT INTO watchlist (user_id, title, movie_id);
VALUE (0001, "Apocalypse Now", 250);


